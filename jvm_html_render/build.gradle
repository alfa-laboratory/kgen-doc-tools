jar {
    baseName = 'html_render'
}

dependencies {
    compile("org.freemarker:freemarker:2.3.27-incubating")

    compile("com.fasterxml.jackson.module:jackson-module-kotlin:2.9.4")

    compile project(":common")
    compile project(":parser")
}